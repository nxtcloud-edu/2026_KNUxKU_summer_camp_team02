# ── 비밀값 — 절대 커밋 금지 ────────────────────────────────
# .env.example 만 예외로 커밋한다 (값이 비어 있는 템플릿)
.env
.env.*
!.env.example
*.env
env.txt
env.local
secrets.*
*.pem
*.key
credentials
aws_credentials

# ── 의존성 · 빌드 산출물 ───────────────────────────────────
node_modules/
dist/
.vite/

# ── MediaPipe WASM (33MB) — postinstall이 만든다 ──────────
public/mediapipe/
studyroom-prototype/public/mediapipe/

# ── Claude Code — 스킬은 용량이 커서 커밋하지 않는다 ────────
.claude/skills/
.claude/settings.local.json
.claude/ui-ux-pro-max-LICENSE

# ── OS · 에디터 ────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp

# ── 로그 ───────────────────────────────────────────────────
*.log
npm-debug.log*
